{
  "id": "airflow",
  "name": "Apache Airflow Orchestrator",
  "description": "Orchestrates data pipelines, triggers DAGs, and monitors workflow tasks.",
  "category": "orchestration",
  "is_data_science": false,
  "parameters": [
    {
      "name": "action",
      "paramType": "String",
      "description": "Action to run: 'trigger_dag', 'get_dag_run_status'",
      "required": true
    },
    {
      "name": "arguments",
      "paramType": "String",
      "description": "JSON string containing parameters defined in contracts.py (e.g., dag_id, conf, run_id)",
      "required": true
    }
  ]
}
